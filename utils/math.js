const sin = angle => Math.sin((Math.PI * angle) / 180).toFixed(3);
const cos = angle => Math.cos((Math.PI * angle) / 180).toFixed(3);
const tan = angle => Math.tan((Math.PI * angle) / 180).toFixed(3);